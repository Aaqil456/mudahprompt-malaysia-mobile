import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mudahprompt.malaysia',
  appName: 'MudahPrompt Malaysia',
  webDir: 'dist'
};

export default config;
